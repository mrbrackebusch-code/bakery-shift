# Cake Factory: mixer and thrown numbers

Current owner direction replaces the two-sided tray workbench with one round central mixing pot. Three feeds approach from the upper left, middle and upper right. The player runs around the pot and throws collected arithmetic blocks into it. These are directional moving projectiles; one impact invokes one learner-authored calculation.

The pot's amount is independent of the bottom orders. Walking onto the number stand copies that amount into a carried numbered container. Throwing this container into an order supplies its RIGHT operand. The fixed LEFT target and comparison sign define acceptance, without an additional allowed range or per-order tool restrictions. YES automatically activates a consistent order; NO leaves another attempt available.

The owner explicitly selected **keep the mixer amount after collecting output**. Subsequent modifiers use this retained value. Orders keep their delivered numbers, and targets remain fixed for the round. Later rounds alternate strict and inclusive comparisons with small targets around the previous amount. The initial completed program has `3 = blank`, `1 >= blank`, and `5 <= blank`: one unchanged number cannot activate all three.

The learner progression contains fourteen constructions: read/display a variable; +1, -1, times two, divide by two; a second operand variable with addition, subtraction, multiplication and division; then Boolean reports for =, <, >, <= and >=. Three gameplay pages retrieve boundary and sequential-calculation practice. Every construction has an exact cumulative program, native editable Blocks, and a corresponding actual-runtime GIF. Seven first-use pages use native Blocks instruction composites; later pages fade support. No learner conditionals, AND/OR, loops or modulus are introduced.

Small examples start at two (the divide-by-three construction starts at three). Subtraction can produce zero and negatives. Division requires a whole-number result. A negative delivered number can help a fixed-target greater-than check but does not automatically solve the opposite comparison. There is no extra hidden maximum or minimum target range.

The world draws at 320 by 240. A shared bowl icon condenses the variable inside red set/purple arithmetic modifiers. The pot and carried output use numbers; each bottom check uses only its target, relation, delivered number and YES/NO feedback. Belts expire missed cards and cycle through all currently installed modifiers. Slow loose cupcakes reuse the add-one event.

The world never substitutes the student's arithmetic or Boolean. Wrong arithmetic remains visible and jams the mixer until reset; a constant false check does not fill an order. A constant true check that contradicts the displayed numbers jams that order instead of silently correcting the Boolean. These observer checks are supplied-world diagnostic behavior, not a learner lesson on conditionals.

Root owns design, integration and informed review. Owner requested bounded cheap mechanical delegation and cancelled learner-agent testing. Runtime, native rendering, source lineage, GIFs, public delivery and human feedback remain distinct evidence. No automated check establishes human enjoyment, learning, accessibility or classroom acceptance.

V6's GitHub v0.2.0 release preceded this new owner correction. Its stable MakeCode entry was still observed serving cached V5; no V6 public native verification pass or review hold was claimed. V7 must verify its own actual public identity before public handoff.
