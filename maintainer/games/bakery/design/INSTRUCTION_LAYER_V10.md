# Cake Factory instruction layer

The owner's September 22 correction makes the guide a primary product surface: keep the real menu blocks and assembled examples, but make annotations carry the teaching instead of duplicating a prose action recipe. Comparison choices should use words and delimited symbols.

The five event dropdown labels now spell out equal to (=), less than (<), greater than (>), less than or equal to (≤), and greater than or equal to (≥). Enum identities, learner variable names, learner programs and game behavior stay the same.

The welcome introduces only the immediate play controls and purpose. Modeled pages retain explicit variable creation and show exact event choices beside native menu blocks. Boxed annotations point to the value, calculation or connection they explain: saving a number, using the saved number, assigning an arithmetic result, and producing a Boolean. Later practice retains the established pattern with less supplied construction detail and recoverable native hints.

The coherent source progression remains the Microsoft variable-math activity and Bakery's retained Boolean/comparison packet. This is a presentation and factual terminology adaptation, not a new progression or a claim of human learning. The writer's source review and the root's rendered integration review are separate records.

All seven modeled examples are freshly rendered through MakeCode's public native Blocks renderer. Exact SVGs and measured geometry are retained; the guide images embed those native shapes. Captions and arrows use measured text and captured bounds. At 303px guide width, wide stacks retain a full overview and enlarged left/right views.

The 17 v0.4.3 gameplay GIFs are reused with explicit code-equivalence evidence. Only block-label metadata changes in the engine; code, learner constructions, controls and visible gameplay are unchanged. This revision does not claim freshly recorded gameplay. The local instruction review embeds every guide image and GIF in one server-free file.

V0.4.3 remains the public baseline until the exact new package, source/visual review, native block validation and release checks finish. Old media and the V7 320x240 rollback remain preserved.
