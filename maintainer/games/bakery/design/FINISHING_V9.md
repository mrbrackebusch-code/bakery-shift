# Cake Factory finishing pass

Owner-requested revision after v0.4.2: express evaluation through a native Boolean result, move confectionery into the upper corner gaps, and make conveyor exits visible.

The B check retains left, comparison and right highlights (320 ms each), then a 200 ms contraction/fade into a pending Boolean shape. Only the learner handler supplies true/false. The result unfolds gently; solved checks retain a compact true block and the evaluated numbers beneath. False returns to the original expression for another try. Missing code never displays an invented false. No flashing or camera shake.

Tiered confectionery shelves occupy the upper left and right gaps. The existing slow manual +1 cupcake sits on a reachable shelf-edge plate; floor confectionery and its trolley are removed. Four ingredient counters stay empty until unlocked.

Each conveyor ends in a dark narrow chute. After four seconds of travel, uncollected modifiers contract in width and height over 600 ms while descending into its slot. Falling cards can no longer be collected; ordinary approach timing is retained.

Keep native 640x480 resolution, Logic blue and pointed Boolean shapes, white numeric inputs, right-input projectile landing, and all learner constructions. Prior 320x240 and v0.4.2 releases remain recoverable. Acceptance uses affected ordinary-input routes, real GIFs and visual review; no learner-agent or human outcome claim.
