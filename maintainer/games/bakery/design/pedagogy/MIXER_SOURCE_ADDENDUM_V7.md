# Bakery Mixer V7 — Source Addendum

This addendum records source grounding only. It does not approve copied tutorial text, prove runtime behavior, or replace the existing source packet.

## Inclusive comparison source

**`MCA-CSINTRO2-LOGIC-MULTIPLAYER` (`1A-043`)** — [official URL](https://arcade.makecode.com/courses/csintro2/logic/multiplayer)  
Local source: `docs/pedagogy/corpus/sources/makecode-arcade-csintro/raw/production/1A-043/repository/docs/courses/csintro2/logic/multiplayer.md`, lines 158–164. The retained examples use “less than or equal to” and “greater than or equal to” checks at a matching boundary. This supports inclusive `<=` and `>=` delivered-number comparisons only; it does not transfer the source’s conditional or `if`/`else if` structure to Bakery learners.

## Existing Bakery bridges

The existing packet’s `MCA-CSINTRO1-INTRO-VARMATH` (`1A-005`) anchor retains the variable bridge, read/calculate/assign/output sequence, changed-number trials, and later reuse, repair, and explanation. The packet’s Boolean source defines Boolean values as `true` or `false`; this addendum does not add new quotations or expand the learner scope beyond the recorded simple Boolean and comparison work.

## Rights

`makecode-arcade-csintro` is MIT. Preserve source provenance and the MIT copyright/permission notice when adapting or redistributing covered source text, code, examples, structure, or repository assets. These obligations are unchanged from `FACTORY_SOURCE_PACKET_2026-09-21.md`.
