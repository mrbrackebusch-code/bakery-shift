# Build the factory as the learner codes

Owner direction, September 22: retain the complete world and its art, but start with static empty belts, the pot, and the movable character. No output number, order statements, loose cake pickup, ingredient variables, or moving belt contents precede their meaningful introduction. Instructions introduce one usable capability at a time and always point at actual native code. Basic interaction becomes arrow keys to move and A to pick up / A to throw. Charging remains an optional later refinement. The live v0.4.6 remains held until this reviewed replacement is ready.

The one visible batch number belongs to the output stand beside the pot. Remove the temporary preview number and duplicate numeric bowl gauge. The stand and carried copied number use a small red variable-style `batch` label. The carried item is a copy: the mixer keeps its value. Every arithmetic handler continues to read the current amount into batch, sets batch to an expression, then applies batch; no change-variable block is introduced.

## Construction sequence

Twenty guide pages: welcome, these sixteen coding steps, then three practice steps. IDs and through indices follow this table. Startup rows add statements to the same native on-start stack. Event identities otherwise retain their existing enum names. The supplied world infers its current teaching phase from installed events, not hidden tutorial-page state or test-only configuration.

| Step / ID | Kind/action | New code / intended visible consequence |
|---|---|---|
| 01-show-batch | startup | batch = 2; bakery.showTray(batch). Reveal 2 at the batch dispenser. |
| 02-start-belt | startup | bakery.setConveyor(true). Middle belt starts with +1 only; one 3 = blank order appears. False visibly stops the belt. |
| 03-add-one | AddOne | Read batch; set batch to batch + 1; apply batch. 2 becomes 3. |
| 04-equal-check | EqualCheck | Read left/right values; ready = orderTarget == deliveredNumber; showCheck(ready). First complete 3 = 3 loop. |
| 05-subtract-one | RemoveOne | Copy addition, change event and + to -. Mixer starts2, equality target1. |
| 06-greater-check | GreaterCheck | Copy equality, change event and = to >. Target2, mixer starts2; subtract1 to make2>1. |
| 07-double | Double | Multiply batch by2. Starts2, equality target4. |
| 08-less-check | LessCheck | Copy greater check, change event and > to <. Target3, starts2; double makes3<4. |
| 09-halve | Halve | Divide batch by2. Starts2, equality target1. |
| 10-add-modifier | AddModifier | Introduce modifierNumber, read the printed input, add it to batch. Starts2, target4. Left belt also starts. |
| 11-subtract-modifier | SubtractModifier | Copy step10 and change event/operator. Starts2, equality target0. |
| 12-multiply-modifier | MultiplyModifier | Copy step10 and change event/operator. Starts2, equality target6. |
| 13-divide-modifier | DivideModifier | Copy step10 and change event/operator. Starts3, equality target1. Right belt also starts. |
| 14-less-equal-check | LessEqualCheck | Copy step8 and change event/operator. Starts2, target2 inclusive comparison. |
| 15-greater-equal-check | GreaterEqualCheck | Copy step6 and change event/operator. Starts2, target2 inclusive comparison. |
| 16-set-batch | SetBatch | Numbered cake2 replaces batch: batch = 2; applyResult(batch). Introduces direct assignment practice without change-by. All three orders become available. |

The first fifteen coding stages use one centrally positioned order (internal order0 maps to display slot1). Its target and relation follow the stage above. Center belt focuses on the latest relevant arithmetic operation. Other belts stay empty/static until their listed activation; after unlock they offer previously coded operations. In the final stage all belts use the full learned set and all three checks/late stations remain available. Side stations stay empty until the first complete three-order round unlocks them.

Pickup is deliberate: pressing A with empty hands picks up one nearby item; that release does not also throw it. A subsequent press/release throws. The output pickup can occur only after its value is revealed. The first guide movement/interaction cue is placed with the first actual pickup task, not an opening control dump. B remains a later optional discard/recheck; mixer reset must invoke the learner's SetBatch event rather than silently diverging from batch. Before that event exists, restart is the recovery path.

Numbers land in an order's right input, retaining the native Boolean shape/color and left-symbol-right-result animation. Automatic delivery checking is the proposed simpler default; a pending owner preference asks whether to keep B-only checking instead. Do not present that preference as resolved until answered or a reasonable optional-response interval has passed.

Every coding addition must have a new actual-runtime GIF showing its visible consequence. New source order, starter, native Blocks, arithmetic/comparison correctness, phase gating, deliberate pickup, batch display and copying, full three-order round, variable stations, wrong-answer recovery and whole-number division require affected checks. Update all native instructions, copy-and-change source references, portable publication and self-contained local review. Preserve v0.4.6 and the V7 rollback. No learner agents under the owner's explicit cancellation; root performs informed checks with bounded Luna specialists for defined tasks.
