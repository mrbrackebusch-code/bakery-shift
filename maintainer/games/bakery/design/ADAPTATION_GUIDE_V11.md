# Copy and change through native code images

The owner's next correction requires visual guidance on every adaptation step: identify the exact earlier event to duplicate, mark only the fields to change, and leave unchanged code unmarked. A hint remains recoverable support, not the primary source of construction directions.

Steps 3, 7, 8, 9, 12, 13 and 14 copy steps 2, 6, 6, 6, 11, 11 and 11 respectively. Each new event changes only the event dropdown and the operation dropdown. Source images retain the actual earlier native event. Its full shape identifies what to duplicate; enlarged event and expression strips support the two edits. The original event remains in the program. The first duplicate introduces the right-click Duplicate affordance.

Steps 15–17 begin with one direct task-and-consequence sentence, followed by the necessary controls and existing gameplay demonstrations. They remain gameplay practice using existing code, not independent coding-transfer assessments.

The engine, learner-state catalog, native hints and gameplay are unchanged from v0.4.4. The native source captures and 17 GIFs are explicitly reused, with exact identity checks. Seven adaptation images extend the guide to 21 instruction images. No compiler, new native renderer export, gameplay recapture or learner-agent run is required for this copy/media-only revision. New visual, editorial, package and public-delivery checks cover the changed surfaces. V0.4.4 is the public baseline until promotion; the owner's request authorizes this replacement while preserving all other game holds.
